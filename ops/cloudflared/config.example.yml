# Copy this file to /home/quyen/.cloudflared/config.yml and keep the
# credentials file generated by `cloudflared tunnel login/create` private.
tunnel: <TUNNEL_ID>
credentials-file: /home/quyen/.cloudflared/<TUNNEL_ID>.json

ingress:
  - hostname: learn.featai.tech
    service: http://localhost:80
  - service: http_status:404
